"use client";import {useState} from "react";import Link from "next/link";
export default function Admin(){const [logged,setLogged]=useState(false);const [form,setForm]=useState({email:"",password:""});const [err,setErr]=useState("");async function login(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(form)});if(!r.ok){setErr("Invalid admin credentials");return}setLogged(true)}if(!logged)return <main><div className="container"><div className="page-head"><span className="eyebrow">Private</span><h1>Admin dashboard</h1></div><form className="panel" style={{maxWidth:520}} onSubmit={login}><label>Email<input className="input" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input className="input" type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{err&&<div className="notice error">{err}</div>}<button className="btn btn-dark">Sign in</button></form></div></main>;return <main><div className="container"><div className="page-head"><span className="eyebrow">Control center</span><h1>Store admin</h1></div><div className="admin-nav"><Link className="btn btn-dark" href="/admin/orders">Orders</Link><Link className="btn btn-outline" href="/admin/products">Products</Link></div><div className="banner"><div><h2>Everything in one place.</h2><p style={{color:"#bbb"}}>Manage your catalog and update order status from the backend.</p></div></div></div></main>}
