"use client";
import {useEffect,useState} from "react";import {useRouter} from "next/navigation";import {money} from "@/lib/utils";
type Item={id:string;name:string;price:number;quantity:number;image?:string};
export default function Checkout(){const router=useRouter();const [items,setItems]=useState<Item[]>([]);const [form,setForm]=useState({customerName:"",email:"",phone:"",address:"",city:"Bharatpur",notes:"",gateway:"COD"});const [loading,setLoading]=useState(false);const [error,setError]=useState("");useEffect(()=>setItems(JSON.parse(localStorage.getItem("panta_cart")||"[]")),[]);const subtotal=items.reduce((s,i)=>s+i.price*i.quantity,0);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");try{const r=await fetch("/api/orders",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...form,items})});const data=await r.json();if(!r.ok)throw new Error(data.error||"Could not create order");localStorage.removeItem("panta_cart");if(data.paymentUrl)window.location.href=data.paymentUrl;else router.push(`/order/${data.orderNumber}`)}catch(err){setError(err instanceof Error?err.message:"Something went wrong");setLoading(false)}}return <main><div className="container"><div className="page-head"><span className="eyebrow">Checkout</span><h1>Complete your order</h1></div>{error&&<div className="notice error">{error}</div>}<form onSubmit={submit} className="cart-layout"><div className="panel"><div className="form-grid"><label>Full name<input className="input" required value={form.customerName} onChange={e=>setForm({...form,customerName:e.target.value})}/></label><label>Email<input className="input" type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Phone<input className="input" required value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>City<input className="input" required value={form.city} onChange={e=>setForm({...form,city:e.target.value})}/></label><label className="full">Address<textarea className="textarea" required value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/></label><label className="full">Notes<textarea className="textarea" value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label><label className="full">Payment<select className="select" value={form.gateway} onChange={e=>setForm({...form,gateway:e.target.value})}><option value="COD">Cash on Delivery</option><option value="ESEWA">eSewa</option><option value="KHALTI">Khalti</option></select></label></div></div><aside className="panel"><b>Order total</b><h2>{money(subtotal)}</h2><p className="muted">{items.length} line item(s)</p><button disabled={loading||!items.length} className="btn btn-dark" style={{width:"100%"}}>{loading?"Processing…":"Place order"}</button></aside></form></div></main>}
